# SPEC.md - ScreenSharing アプリケーション仕様書

## 1. 概要
同一マシン上で、異なるOSユーザーアカウント間で画面共有を行うデスクトップアプリケーション。
ネットワーク通信は一切使用せず、マシン内の共有一時領域(`/tmp`相当)にフレームデータを
書き込み/読み込みすることで画面共有を実現する。

- 対象OS: macOS, Linux (Windows対象外)
- 言語/フレームワーク: C++17 / Qt 5.15
- ビルド: CMake
- コーディング規約: `CLAUDE.md` (Google C++ Style Guide, メンバー変数は末尾アンダースコア `member_`)

## 2. 用語
| 用語 | 意味 |
|---|---|
| 共有側 (Presenter) | 自分の画面をキャプチャして公開するユーザー/アプリインスタンス |
| 閲覧側 (Viewer) | 他者の画面を閲覧するユーザー/アプリインスタンス |
| 招待コード | 4桁の数字。セッションの識別子であり、暗号鍵の種にもなる |
| セッション | 1回の共有開始(Start)〜停止(Stop)までの単位。招待コード1つにつき1セッション |

## 3. トポロジー
- 1人が共有 (Presenter) → 複数アカウントが同時に閲覧可能 (1対多)。
- 1つのアプリケーションバイナリが共有側/閲覧側の両モードを持ち、UI上のボタンで切り替える。
- 同一マシン上で複数ユーザーが同時に別々のセッションを共有することも可能(各自が異なる招待コードを持つ)。

## 4. 全体フロー

### 4.1 共有側 (Presenter)
1. ユーザーがアプリを起動し、「共有」モードを選択。
2. アプリが4桁の招待コードを自動生成して表示する(コード衝突時は再生成、または既存セッションが
   生存していれば別コードを提案する)。
3. ユーザーが解像度を選択(プリセットまたはスライダー)。
4. 「開始」ボタン押下で画面キャプチャとフレーム書き込みを開始。
5. 実行中は実効フレームレート・書き込みスループットを画面に表示し、ユーザーはこれを見ながら
   いつでも解像度を変更できる。
6. 「停止」ボタン押下でキャプチャを止め、セッションディレクトリをクリーンアップする。

### 4.2 閲覧側 (Viewer)
1. ユーザーがアプリを起動し、「閲覧」モードを選択。
2. 4桁の招待コードを入力し「接続」。
3. アプリは共有領域内から該当コードのセッションディレクトリを探索し、見つかれば復号しつつ
   フレームを表示する。見つからない/コード不一致の場合はエラー表示。
4. 共有側が停止した、またはハートビートが一定時間途絶えた場合、「切断されました」を表示する。

### 4.3 ログ
- 共有側・閲覧側どちらでも、メニュー/ボタンから「ログ」ダイアログを開ける。
- ダイアログには実行状況(セッション開始/停止、招待コード発行、実測fps・スループット、
  エラー、切断検知など)をタイムスタンプ付きで表示する。ログはアプリ内メモリ上のみ保持で良く、
  永続化は必須要件としない。

## 5. ファイル/ディレクトリレイアウト(共有一時領域)

```
<共有ベースディレクトリ>/          例: /tmp/qt_screensharing/
  └── <code>/                     例: 4桁の招待コードそのものをディレクトリ名にする
        ├── meta.json             セッションメタデータ
        ├── heartbeat             最終更新時刻のみを持つファイル(生存確認用)
        ├── frame_0.bin           ダブルバッファ用フレーム(暗号化済み)
        └── frame_1.bin
```

- `meta.json` には現在のフレーム幅・高さ(=共有側のキャプチャ解像度、縦横比の算出元)を含める。
  共有側が解像度を変更するたびにこの値も更新する。

- 共有ベースディレクトリは `/tmp` 同様に **sticky bit 付き world-writable (mode 1777)** で
  作成し、どのアカウントでも自分のサブディレクトリを作れるようにする。
- 各セッションディレクトリ・ファイルは **world-readable** にする(異なるOSアカウント間には
  共通グループがない前提のため、パーミッションだけでのアクセス制御はできない)。
  → アクセス制御は招待コードによる暗号化(6章)で担保する。
- フレーム書き込みは「一時ファイルに書いてから `rename()`」でアトミックに行い、閲覧側が
  書き込み途中の不完全なフレームを読まないようにする。`meta.json` 内のシーケンス番号で
  最新フレームがどちらのバッファかを示す。
- `heartbeat` は共有側が一定間隔(例: 1秒)ごとに更新時刻を書き込む。閲覧側はこれが
  一定時間(例: 3秒)更新されなければセッション消失と判断する。
- 「停止」時、共有側は自身のセッションディレクトリを削除する。異常終了時に残ったディレクトリは
  次回起動時またはハートビート期限切れ時にクリーンアップ対象とする。

## 6. セキュリティモデル
- ネットワークを使わないため認証はマシンローカルの4桁招待コードのみ。
- 招待コードから簡易ストリーム暗号(コードを種としたXORキーストリーム)を生成し、
  フレームデータおよびmeta.json中の機微情報を暗号化する。
- 想定する脅威: **同一マシン上の無関係な他ユーザーによる覗き見**。招待コードを知らない
  第三者が共有領域を`ls`しても、中身は復号できない。
- 想定しない脅威: 悪意ある攻撃者による総当たり(4桁=1万通りのため強度は低い)。この用途は
  「同じ部屋の同僚に画面を見せる」程度のカジュアルな用途を想定し、暗号強度はそれに見合う
  ものとする。
- 外部暗号ライブラリは使用しない(追加依存なし)。

## 7. キャプチャ/転送仕様
- フォーマット: 生ビットマップ(エンコードなし)。
- デフォルトフレームレート: 15fps固定。
- 解像度: 共有側がプリセットまたはスライダーで調整可能。実測スループット/fpsを見ながら
  ユーザーが判断する。
- キャプチャAPI:
  - macOS: ScreenCaptureKit (要: 初回のみOSの画面収録許可ダイアログ。sudo不要)
  - Linux: X11 (`XGetImage`等)。Waylandは対象外(xdg-desktop-portal経由の追加許可フローが
    必要なため、初期スコープでは扱わない)。

## 8. UI構成(概要)
- 起動時: モード選択(共有 / 閲覧)。
- 共有パネル:
  - 招待コード表示
  - 解像度選択(プリセット/スライダー)
  - 実効fps・スループット表示
  - 開始/停止ボタン
  - ログを見るボタン
- 閲覧パネル:
  - 招待コード入力欄 + 接続ボタン
  - 映像表示エリア
  - 接続状態表示(接続中/切断)
  - ログを見るボタン

### 8.1 映像表示エリアの縦横比
- 送信側(共有側)ディスプレイと受信側(閲覧側)ディスプレイの縦横比は異なりうる。
- 受信側の映像表示エリアは、`meta.json`で通知された**送信側の縦横比を維持**したまま
  表示する(レターボックス/ピラーボックス方式で、余白は黒帯とする)。表示エリアいっぱいに
  引き伸ばして送信側の縦横比を崩すことはしない。
- 送信側が解像度を変更し縦横比が変化した場合、受信側は次に受信するフレームから
  新しい縦横比に追従して再レイアウトする。

## 9. 非機能要件
- sudoコマンド・昇格権限を一切使用しない。
- ビルドは `-Wall -Wextra -Werror` で警告ゼロ(`CLAUDE.md`準拠)。
- スムージング/メモリ管理はスマートポインタ/値セマンティクスを用いる(生の`new`/`delete`禁止)。

## 10. スコープ外(初期バージョンでは対応しない)
- Windows対応
- Wayland対応
- リモート操作(マウス/キーボード操作の転送)
- ネットワーク経由(別マシン間)の共有
- H.264等のエンコード
- アカウント登録型の認証、暗号の強化(AES等)
- 閲覧側が誰であるかを共有側が把握する仕組み(視聴者数表示等)
